#!/usr/bin/env bash
# Configure NGINX to responsd with custom header

sudo apt-get -y update
sudo apt-get install -y nginx
sed -i '16iadd_header X-Served-By $hostname;' /etc/nginx/nginx.conf
sudo service nginx restart
